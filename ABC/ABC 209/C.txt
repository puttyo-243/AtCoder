#長さNの数列Aの個数
range(1,N+1)の二重ループ => 計算量が多くなるからだめ
Cのリストから長さNの数列をすべて抽出(組み合わせ)（ここで制約を条件に抽出できれば...？）
それぞれの数列が制約に一致するか判定

Aの最低値は1、最大値はA <= CN
Aの取りうる値のリスト作成（[x for x in range(1,C[:-1])]）
AのリストからN個取る組み合わせ（combination使わない）（ Ai<Aj が成り立つように取る）
